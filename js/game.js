import { getSupabase, databaseSelect, databaseInsert, databaseUpdate } from "./supabase.js";
import { buildBoard, CARD_TYPES } from "./cards.js";

const DEFAULTS = Object.freeze({
    classic: { boardSize: 25, targetScore: 7, maxPlayers: 8, bonus: false, darkCards: 1 },
    expanded: { boardSize: 35, targetScore: 10, maxPlayers: 8, bonus: true, darkCards: 1 },
    chaos: { boardSize: 36, targetScore: 12, maxPlayers: 8, bonus: true, darkCards: 2 },
    duel: { boardSize: 16, targetScore: 5, maxPlayers: 2, bonus: false, darkCards: 1 }
});
function settingsFor(mode, overrides={}){return{...(DEFAULTS[mode]||DEFAULTS.classic),...overrides};}
function createRoomCode(){const chars="ABCDEFGHJKLMNPQRSTUVWXYZ23456789";let code="";for(let i=0;i<6;i++)code+=chars[Math.floor(Math.random()*chars.length)];return code;}
async function uniqueRoomCode(){for(let attempt=0;attempt<12;attempt++){const code=createRoomCode();const found=await databaseSelect("rooms",{eq:{code},limit:1});if(!found.length)return code;}throw new Error("Could not create a unique room code.");}
function normalizeState(room,players=[]){const state=room.game_state&&typeof room.game_state==="object"?room.game_state:{};return{...state,roomId:room.id,status:room.status,mode:room.game_mode||"classic",settings:room.settings||{},currentTurn:room.current_turn,turnTeam:room.turn_team,winnerTeam:room.winner_team,players};}
function playerFormat(settings={}, mode="classic"){return settings.playerFormat || (mode==="duel" ? "1v1" : "2v2");}
function requiredPlayers(settings={}, mode="classic"){return playerFormat(settings,mode)==="1v1"?2:4;}
export async function createGame({userId,mode="classic",maxPlayers=8,boardSize,playerFormat:format="2v2"}={}){if(!userId)throw new Error("Authentication required.");const normalizedFormat=format==="1v1"?"1v1":"2v2";const chosen=settingsFor(mode,{maxPlayers:normalizedFormat==="1v1"?2:Math.max(4,Number(maxPlayers)||8),...(boardSize?{boardSize}:{}),playerFormat:normalizedFormat});const roomCode=await uniqueRoomCode();const roomState={board:[],turnNumber:1,clue:null,guessesLeft:0,scores:{red:0,blue:0},startedAt:null,phase:"lobby",clueGiver:null,guesser:null,teamRoles:{red:{clueGiver:null,guessers:[]},blue:{clueGiver:null,guessers:[]}}};const rooms=await databaseInsert("rooms",{code:roomCode,host_id:userId,status:"waiting",max_players:chosen.maxPlayers,turn_team:"red",game_state:roomState,game_mode:mode,settings:chosen});const room=rooms[0];if(!room?.id)throw new Error("Room creation failed.");const profile=await databaseSelect("profiles",{eq:{id:userId},limit:1});const displayName=profile[0]?.username||"Player";await databaseInsert("room_players",{room_id:room.id,user_id:userId,display_name:displayName,team:"red",is_host:true});return{roomId:room.id,roomCode,settings:chosen};}
export async function joinGame({code,userId}={}){if(!code||!userId)throw new Error("Room code and user are required.");const rooms=await databaseSelect("rooms",{eq:{code:code.trim().toUpperCase()},limit:1});const room=rooms[0];if(!room)throw new Error("Room not found.");if(room.status!=="waiting")throw new Error("This room has already started.");const existing=await databaseSelect("room_players",{eq:{room_id:room.id,user_id:userId},limit:1});if(!existing.length){const players=await databaseSelect("room_players",{eq:{room_id:room.id}});if(players.length>=room.max_players)throw new Error("Room is full.");const profile=await databaseSelect("profiles",{eq:{id:userId},limit:1});const displayName=profile[0]?.username||"Player";const redCount=players.filter(p=>p.team==="red").length;const blueCount=players.filter(p=>p.team==="blue").length;const team=redCount<=blueCount?"red":"blue";await databaseInsert("room_players",{room_id:room.id,user_id:userId,display_name:displayName,team,is_host:false});}return{roomId:room.id,roomCode:room.code};}
async function loadPlayers(roomId){return databaseSelect("room_players",{eq:{room_id:roomId},order:{column:"joined_at",ascending:true}});}
export async function getGameState(roomId){const rooms=await databaseSelect("rooms",{eq:{id:roomId},limit:1});if(!rooms[0])return null;const players=await loadPlayers(roomId);const roomState=normalizeState(rooms[0],players);if(roomState.matchId){const cards=await databaseSelect("match_cards",{eq:{match_id:roomState.matchId},order:{column:"position",ascending:true}});const clues=await databaseSelect("match_clues",{eq:{match_id:roomState.matchId},order:{column:"turn_number",ascending:true}});roomState.cards=cards;roomState.clues=clues;if(!roomState.clue&&clues.length){const latest=clues[clues.length-1];roomState.clue={text:latest.clue_text,number:latest.clue_number,team:latest.team,turnNumber:latest.turn_number};}}return roomState;}
function teamPlayers(players,team){return players.filter(p=>p.team===team);}
function makeTeamRoles(players,team){const members=teamPlayers(players,team);const clueGiver=members[0]?.user_id||null;const guessers=members.length>1?members.slice(1).map(p=>p.user_id):members.map(p=>p.user_id);return{clueGiver,guessers};}
function makeRoles(players){return{red:makeTeamRoles(players,"red"),blue:makeTeamRoles(players,"blue")};}
function currentClueGiver(roles,team){return roles?.[team]?.clueGiver||null;}
function currentGuessers(roles,team){return Array.isArray(roles?.[team]?.guessers)?roles[team].guessers:[];}
function buildMatchBoard(size,mode,firstTeam){return buildBoard(size,mode,firstTeam);}
export async function startGame({roomId,userId}={}){const rooms=await databaseSelect("rooms",{eq:{id:roomId},limit:1});const room=rooms[0];if(!room||room.host_id!==userId)throw new Error("Only the room host can start the match.");if(room.status!=="waiting")throw new Error("Match is not waiting.");const players=await loadPlayers(roomId);const format=playerFormat(room.settings||{},room.game_mode||"classic");const minimum=requiredPlayers(room.settings||{},room.game_mode||"classic");if(players.length<minimum)throw new Error(format==="1v1"?"برای شروع 1 به 1 باید ۲ بازیکن وارد اتاق باشند.":"برای شروع 2 به 2 باید حداقل ۴ بازیکن وارد اتاق باشند.");if(!players.some(p=>p.team==="red")||!players.some(p=>p.team==="blue"))throw new Error("Both teams need at least one player.");if(format==="2v2"&&(players.filter(p=>p.team==="red").length<2||players.filter(p=>p.team==="blue").length<2))throw new Error("در حالت 2 به 2 هر تیم باید حداقل ۲ بازیکن داشته باشد.");const mode=room.game_mode||"classic";const settings=settingsFor(mode,room.settings||{});const board=buildMatchBoard(settings.boardSize,mode,"red");const matches=await databaseInsert("matches",{room_id:roomId,game_mode:mode,board_size:settings.boardSize,target_score:settings.targetScore,round_number:1,started_at:new Date().toISOString()});const match=matches[0];if(!match?.id)throw new Error("Could not create match.");await databaseInsert("match_players",players.map(p=>({match_id:match.id,user_id:p.user_id,team:p.team,score:0})));await databaseInsert("match_cards",board.map((card,position)=>({match_id:match.id,position,card_id:null,card_type:card.type,revealed:false,reveal_value:card.type===CARD_TYPES.BONUS?2:1})));const roles=makeRoles(players);const clueGiver=currentClueGiver(roles,"red");const initialGuessers=currentGuessers(roles,"red");const firstGuesser=initialGuessers[0]||clueGiver;const nextState={board,matchId:match.id,turnNumber:1,clue:null,guessesLeft:0,scores:{red:0,blue:0},startedAt:new Date().toISOString(),revealed:[],usedClues:[],phase:"clue",clueGiver,guesser:firstGuesser,teamRoles:roles,playerFormat:format};await databaseUpdate("rooms",{status:"playing",current_turn:clueGiver,turn_team:"red",game_state:nextState,winner_team:null},{id:roomId});await databaseInsert("match_events",{match_id:match.id,actor_id:userId,event_type:"match_started",payload:{mode,boardSize:settings.boardSize,clueGiver,firstGuesser,playerFormat:format}});return{matchId:match.id,gameState:nextState};}
function teamForUser(players,userId){return players.find(p=>p.user_id===userId)?.team||null;}
export async function submitClue({roomId,userId,text,number}={}){const state=await getGameState(roomId);if(!state||state.status!=="playing")throw new Error("Match is not active.");if(state.phase!=="clue")throw new Error("الان زمان دادن سرنخ نیست.");const team=teamForUser(state.players||[],userId);if(!team||state.turnTeam!==team)throw new Error("You are not on the active team.");if(state.clueGiver!==userId||currentClueGiver(state.teamRoles,state.turnTeam)!==userId)throw new Error("Only the clue giver can submit the clue.");const clueText=String(text||"").trim().slice(0,40);const clueNumber=Number(number);const maxNumber=Math.max(1,Math.min((state.board||[]).length,9));if(!clueText)throw new Error("سرنخ را وارد کن.");if(!Number.isInteger(clueNumber)||clueNumber<1||clueNumber>maxNumber)throw new Error(`عدد سرنخ باید بین ۱ تا ${maxNumber} باشد.`);await databaseInsert("match_clues",{match_id:state.matchId,team:state.turnTeam,turn_number:state.turnNumber||1,clue_text:clueText,clue_number:clueNumber,created_by:userId});const guessers=currentGuessers(state.teamRoles,state.turnTeam);const nextUser=guessers.find(id=>id!==userId)||userId;if(!nextUser)throw new Error("حداقل یک پیدا کننده در تیم لازم است.");const nextState={...state,clue:{text:clueText,number:clueNumber,team:state.turnTeam,turnNumber:state.turnNumber||1},guessesLeft:clueNumber,phase:"guess",currentTurn:nextUser,guesser:nextUser,clueGiver:userId,usedClues:[...(state.usedClues||[]),clueText]};await databaseUpdate("rooms",{current_turn:nextUser,turn_team:state.turnTeam,game_state:nextState},{id:roomId});await databaseInsert("match_events",{match_id:state.matchId,actor_id:userId,event_type:"clue_submitted",payload:{clue:clueText,number:clueNumber,team:state.turnTeam,clueGiver:userId,guesser:nextUser}});return getGameState(roomId);}
function checkWinner(scores,targetScore,remaining){if(scores.red>=targetScore)return"red";if(scores.blue>=targetScore)return"blue";if(remaining<=0)return scores.red===scores.blue?null:(scores.red>scores.blue?"red":"blue");return null;}
export async function selectCard({roomId,userId,cardId}={}){const state=await getGameState(roomId);if(!state||state.status!=="playing")throw new Error("Match is not active.");if(state.phase==="clue")throw new Error("ابتدا سرنخ و تعداد کارت را ثبت کن.");const guessers=currentGuessers(state.teamRoles,state.turnTeam);if(!(guessers.includes(userId)||state.guesser===userId||state.currentTurn===userId))throw new Error("الان نوبت پیدا کننده است.");if(!Array.isArray(state.cards))throw new Error("Board is unavailable.");const players=state.players||[];const team=teamForUser(players,userId);if(!team||team!==state.turnTeam)throw new Error("You are not on the active team.");if(state.guessesLeft<=0)throw new Error("تعداد حدس‌های این سرنخ تمام شده است.");const card=state.board.find(c=>c.id===cardId)||state.board.find(c=>String(c.position)===String(cardId));if(!card)throw new Error("Card not found.");const position=Number(card.position);const matchCard=state.cards.find(c=>Number(c.position)===position);if(!matchCard||matchCard.revealed)throw new Error("Card is already revealed.");const nextScores={...(state.scores||{red:0,blue:0})};const nextRevealed=[...(state.revealed||[]),position];let turnEnded=false;let gameFinished=false;let points=0;if(card.type===team)points=card.bonus?2:1;if(card.type===CARD_TYPES.NEUTRAL)points=0;if(card.type===CARD_TYPES.DARK)points=0;nextScores[team]=Math.max(0,(nextScores[team]||0)+points);await databaseUpdate("match_cards",{revealed:true,revealed_by:userId,reveal_value:points},{match_id:state.matchId,position});await databaseInsert("match_events",{match_id:state.matchId,actor_id:userId,event_type:"card_revealed",payload:{position,type:card.type,points}});const totalRemaining=state.board.filter(c=>!nextRevealed.includes(c.position)&&c.type!==CARD_TYPES.DARK).length;const winner=checkWinner(nextScores,state.settings?.targetScore||7,totalRemaining);if(winner){gameFinished=true;turnEnded=true;await databaseUpdate("rooms",{status:"finished",winner_team:winner,current_turn:userId,game_state:{...state,scores:nextScores,revealed:nextRevealed,turnEnded:true,phase:"finished"}},{id:roomId});await databaseUpdate("matches",{winner_team:winner,red_score:nextScores.red,blue_score:nextScores.blue,finished_at:new Date().toISOString()},{id:state.matchId});try{const supabase=getSupabase();const{data:rewardData,error:rewardError}=await supabase.rpc("award_match_rewards",{p_match_id:state.matchId,p_winner_team:winner});if(rewardError)throw rewardError;console.log("CODNAME rewards applied",rewardData);window.dispatchEvent(new CustomEvent("codname:rewards-applied",{detail:{matchId:state.matchId,winnerTeam:winner}}));}catch(error){console.error("CODNAME reward update failed",error);window.dispatchEvent(new CustomEvent("codname:rewards-error",{detail:{message:error?.message||"ثبت XP و برد انجام نشد."}}));}return{turnEnded,gameFinished,gameState:await getGameState(roomId)};}const nextGuesses=Math.max(0,(state.guessesLeft||1)-1);const wrongCard=card.type!==team||card.type===CARD_TYPES.DARK||card.type===CARD_TYPES.NEUTRAL;if(wrongCard||nextGuesses===0){turnEnded=true;const nextTeam=state.turnTeam==="red"?"blue":"red";const roles=state.teamRoles||makeRoles(players);const nextClueGiver=currentClueGiver(roles,nextTeam);const nextGuessers=currentGuessers(roles,nextTeam);const nextState={...state,scores:nextScores,revealed:nextRevealed,clue:null,guessesLeft:0,phase:"clue",turnNumber:(state.turnNumber||1)+1,clueGiver:nextClueGiver,guesser:nextGuessers[0]||nextClueGiver,teamRoles:roles};await databaseUpdate("rooms",{current_turn:nextClueGiver,turn_team:nextTeam,game_state:nextState},{id:roomId});return{turnEnded,gameFinished,gameState:await getGameState(roomId)};}const roles=state.teamRoles||makeRoles(players);const teamGuessers=currentGuessers(roles,team);const nextUser=teamGuessers[0]||userId;const nextState={...state,scores:nextScores,revealed:nextRevealed,guessesLeft:nextGuesses,currentTurn:nextUser,guesser:nextUser,phase:"guess"};await databaseUpdate("rooms",{current_turn:nextUser,turn_team:team,game_state:nextState},{id:roomId});return{turnEnded,gameFinished,gameState:await getGameState(roomId)};}
export async function endTurn({roomId,userId}={}){const state=await getGameState(roomId);if(!state||state.status!=="playing")throw new Error("Match is not active.");if(state.phase!=="guess")throw new Error("ابتدا سرنخ را ثبت کن.");const guessers=currentGuessers(state.teamRoles,state.turnTeam);if(!(guessers.includes(userId)||state.currentTurn===userId))throw new Error("It is not your turn.");const players=state.players||[];const nextTeam=state.turnTeam==="red"?"blue":"red";const roles=state.teamRoles||makeRoles(players);const nextClueGiver=currentClueGiver(roles,nextTeam);const nextGuessers=currentGuessers(roles,nextTeam);const nextState={...state,clue:null,guessesLeft:0,phase:"clue",turnNumber:(state.turnNumber||1)+1,clueGiver:nextClueGiver,guesser:nextGuessers[0]||nextClueGiver,teamRoles:roles};await databaseUpdate("rooms",{current_turn:nextClueGiver,turn_team:nextTeam,game_state:nextState},{id:roomId});await databaseInsert("match_events",{match_id:state.matchId,actor_id:userId,event_type:"turn_ended",payload:{nextTeam,nextUser:nextClueGiver}});return getGameState(roomId);}
export { DEFAULTS };
